spring:
  config:
    activate:
      on-profile: production
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${ESQUILO_JWT_ISSUER_URI}
          audiences: ${ESQUILO_JWT_AUDIENCE}
